# inbox-triage Filing Rules
# This file is populated by /inbox-triage:learn and updated during triage
# Copy to filing-rules.yaml to start fresh

# Rule configuration
config:
  min_confidence_to_suggest: 0.70
  confidence_boost_on_confirm: 0.05
  confidence_decay_on_reject: 0.15
  deprecate_after_rejections: 3

  priority_order:
    - sender_email    # Exact sender match
    - sender_domain   # Domain-based match
    - subject_pattern # Subject line regex
    - combined        # Multiple criteria

# Learned filing rules
rules:
  sender_domain:
    # Example rule:
    # - domain: "example.com"
    #   target_folder: "Work"
    #   target_folder_id: "ABC123"
    #   confidence: 0.85
    #   match_count: 10
    #   source: "bootstrap"  # or "user_correction", "manual"
    []

  sender_email: []

  subject_pattern:
    # Example rule:
    # - pattern: "Your order has shipped"
    #   target_folder: "Orders"
    #   target_folder_id: "XYZ789"
    #   confidence: 0.90
    #   match_count: 5
    #   source: "bootstrap"
    []

  combined: []

# Rule statistics
statistics:
  total_rules: 0
  rules_by_source:
    bootstrap: 0
    user_correction: 0
    manual: 0
  last_updated: null
