# Chief-of-Staff Email Action Routes
# Routes emails to specialized skills/agents for active processing
# Think of it as: filing rules route to folders; action routes route to skills.
#
# Routes are checked in priority order: sender_email > sender_domain > subject_pattern > combined
# Matched routes surface as "Process: [label]" in triage (batch and interactive)

metadata:
  last_updated: null
  total_routes: 0

# Confidence thresholds (higher than filing rules — routing is more consequential)
thresholds:
  suggest_minimum: 0.80      # Minimum confidence to suggest routing in triage
  auto_route_minimum: 0.95   # Future: auto-process without asking
  deprecate_below: 0.50      # Routes below this are flagged for review

routes:
  # Exact sender email matches (highest priority)
  sender_email:
    # - email: "Accounting@localforeigner.com"
    #   subject_pattern: null           # Optional: additional subject filter
    #   attachment_required: true        # Optional: only match if email has attachments
    #   route:
    #     plugin: "chief-of-staff-private"
    #     agent: "local-foreigner-invoice"  # OR skill: "skill-name"
    #     label: "Process LF Invoice"       # Human-readable name shown in triage
    #     description: "Extract invoice, summarize by trip, generate YNAB split"
    #     pass_attachments: true            # Fetch attachment metadata before invoking
    #     post_action: "archive"            # What to do after: archive, delete, keep, none
    #     post_action_folder: "Local Foreigner"  # Folder name (for archive post_action)
    #     post_action_folder_id: null       # Folder ID (optional, looked up if missing)
    #   confidence: 0.95
    #   match_count: 0
    #   source: "manual"                    # manual, learned, migrated
    #   enabled: true
    []

  # Domain-level route matches
  sender_domain:
    # - domain: "phamatech.com"
    #   subject_pattern: "Result Available"
    #   route:
    #     plugin: "chief-of-staff-private"
    #     skill: "phamatech-download"
    #     label: "Download Phamatech Results"
    #     description: "Download drug test verification PDF"
    #     pass_attachments: false
    #     post_action: "archive"
    #   confidence: 0.95
    #   match_count: 0
    #   source: "manual"
    #   enabled: true
    []

  # Subject pattern route matches
  subject_pattern:
    # - pattern: "NetJets Invoice"
    #   sender_pattern: "billing@netjets.com"  # Optional: restrict by sender
    #   route:
    #     plugin: "chief-of-staff-private"
    #     agent: "netjets-invoice-downloader"
    #     label: "Download NetJets Invoices"
    #     pass_attachments: false
    #     post_action: "none"
    #   confidence: 0.90
    #   match_count: 0
    #   source: "manual"
    #   enabled: true
    []

  # Combined route matches (domain + subject)
  combined:
    # - domain: "example.com"
    #   subject_pattern: "monthly report"
    #   route:
    #     plugin: "my-plugin"
    #     agent: "report-processor"
    #     label: "Process Monthly Report"
    #     pass_attachments: true
    #     post_action: "archive"
    #     post_action_folder: "Reports"
    #   confidence: 0.85
    #   match_count: 0
    #   source: "learned"
    #   enabled: true
    []
